services:
  messageboard:
    build: .
    image: messageboard:local
    ports:
      - "8080:8080"
    volumes:
      - messageboard-data:/data
    environment:
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-}
    restart: unless-stopped

volumes:
  messageboard-data:
