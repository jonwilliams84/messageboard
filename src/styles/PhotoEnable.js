import styled, { css } from "styled-components";

const PhotoEnable = styled.div`
  width: 180px;
  height: 40px;
  flex-direction: row;
  display: flex;
  margin-top: 30px;
  align-items: center;
  text-align: center;
  margin-left: 0 auto;
`;

export default PhotoEnable;